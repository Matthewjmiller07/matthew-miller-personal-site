[build]
  command = "npm install --legacy-peer-deps && npm run sync:scp && astro build"
  publish = "dist"

[functions]
  included_files = [
    "node_modules/node-html-parser/**",
    "public/images/Sukkah Posters/**"
  ]
