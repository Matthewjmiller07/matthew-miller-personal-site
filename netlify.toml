[build]
  command = "npm install --legacy-peer-deps && npm run sync:scp && node scripts/extract-pdf-links.mjs && astro build"
  publish = "dist"

[functions]
  included_files = [
    "node_modules/node-html-parser/**",
    "public/images/Sukkah Posters/**",
    "public/aviya.csv"
  ]
